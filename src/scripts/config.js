const width = 1920;
const height = 1080;
const config = {
    width: width,
    height: height,
    centerX: width / 2,
    centerY: height / 2,
    version: 1.0,
    fontFamily1: 'Arial',
};

export default config;